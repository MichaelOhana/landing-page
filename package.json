{
  "name": "real_estate_course",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page & course frontend built with Vite",
  "scripts": {
    "dev": "vite",
    "build": "vite build --outDir dist --emptyOutDir",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}